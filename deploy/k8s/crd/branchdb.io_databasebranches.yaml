---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: databasebranches.branchdb.io
spec:
  group: branchdb.io
  names:
    kind: DatabaseBranch
    listKind: DatabaseBranchList
    plural: databasebranches
    singular: databasebranch
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.externalPort
      name: ExternalPort
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DatabaseBranch は BranchDB が管理するデータベースブランチを表す。
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DatabaseBranchSpec は DatabaseBranch の望ましい状態を定義する。
            properties:
              databaseType:
                description: "DatabaseType は起動するデータベースの種類。対応値: mysql（デフォルト）, postgres, redis"
                type: string
                default: mysql
              databaseVersion:
                description: DatabaseVersion はコンテナイメージのタグを上書きする。省略時は Operator のデフォルトを使用する。
                type: string
              initSQL:
                description: InitSQL はブランチ作成後に実行する初期化 SQL を指定する。
                type: string
              snapshotRef:
                description: SnapshotRef はブランチの元となるスナップショット名を指定する。
                type: string
              ttlHours:
                description: TTLHours はブランチの有効期間（時間）を指定する。0 は無期限を意味する。
                type: integer
            required:
            - snapshotRef
            type: object
          status:
            description: DatabaseBranchStatus は DatabaseBranch の観測された状態を定義する。
            properties:
              clusterHost:
                description: ClusterHost はクラスター内からアクセスするためのホスト名を示す。
                type: string
              clusterPort:
                description: ClusterPort はクラスター内からアクセスするためのポート番号を示す。
                type: integer
              expiresAt:
                description: ExpiresAt はブランチの有効期限を示す。
                format: date-time
                type: string
              externalHost:
                description: ExternalHost はクラスター外からアクセスするためのホスト名を示す。
                type: string
              externalPort:
                description: ExternalPort はクラスター外からアクセスするためのポート番号を示す。
                type: integer
              message:
                description: Message は現在の状態に関するメッセージを示す。
                type: string
              phase:
                description: Phase はブランチの現在のライフサイクルフェーズを示す。
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
