{{- if and .Values.zfsAgent.token (not .Values.zfsAgent.existingSecret) -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "branchdb-operator.secretName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "branchdb-operator.labels" . | nindent 4 }}
type: Opaque
stringData:
  zfsagent-token: {{ .Values.zfsAgent.token | quote }}
{{- end }}
